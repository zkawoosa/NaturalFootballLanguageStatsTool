# NFL NLP Stats App — 28-Day Plan (1h/day)

## Purpose
Build a local-first TypeScript/Next.js NFL natural-language stats site, then deploy.

## Day 1–28 Execution Template

### Day 1: Product contract lock
- [X] Create `project-spec.md` (supported query types, response format, unsupported behavior, success criteria).
- [X] Define “MVP done” checklist.
- [X] Confirmed DB direction and added SQLite choice to project spec.

### Day 2: Data source validation
- [X] Verify chosen public NFL source end-to-end (latency, rate limit, fields, failures).
- [X] Save 10 raw sample responses for teams/players/stats.
- [X] Pick source and record base URL, path, limits, supported seasons.
- [X] Capture raw schema mapping draft: raw -> canonical entities.
- [X] Confirm failure handling for timeout / 429 / 5xx / empty responses.
- [X] Add Day 2 runtime logging template for data-source calls (timestamp, endpoint, status, latency, source, retries, rate-limit hits).
- [X] Add source logger utility (`src/lib/logger.ts`) and wire it into source adapter calls, with stdout fallback + optional file sink.
- [X] Deliverable: source decision + raw samples + risk list.

### Day 3: Canonical schema v1
- [X] Define canonical entities: `team`, `player`, `game`, `teamStat`, `playerStat` (drafted in `src/lib/schema/canonical.ts`).
- [X] Decide ID strategy, date format, units, null handling.
  - [X] Internal precision for derived decimals/rates is 6 dp.
  - [X] Display precision for public output is 4 dp via `roundForDisplay`.

### Day 4: Data adapter contract
- [X] Define `IDataSource` interface and method names/signatures.
- [X] Define standardized error codes and retry policy (strict: retry only 429 with backoff).
- [X] Confirm parser will never call source methods directly (use mapper layer).
- [X] Add and run source adapter tests for rate-limit retry/exhaustion and a quiet full-test suite.

### Day 5: Parser vocabulary v1
- [X] Define intents (`leaders`, `player_stat`, `team_stat`, `weekly_summary`, `compare`).
- [X] Build alias maps for teams/stats and ambiguity policy.

### Day 6: QA corpus baseline [X]
- [X] Enforce review priorities:
  - [X] P0/P1 issues must be reported to user and require explicit permission before merge.
  - [X] P2/P3 are auto-corrected before the next checkpoint.
- [X] Add linting (e.g., npm script + baseline rules) before corpus completion.

### Day 7: Environment + workflow
- [X] Create `.env.example` for source selection, cache, timeouts.
- [X] Lock Node/PNPM versions and script names (`dev`, `build`, `lint`, optional `start`).

### Day 8: API/UX contracts [X]
- [X] Define all API request/response shapes (`/api/query`, `/api/teams`, `/api/players`, `/api/status`).
- [X] Design UI response states: success/loading/error/empty/clarification.
- [X] Document route index.

### Day 9: Reliability plan [X]
- [X] Define logging fields (query, intent, confidence, latency, cache hit, source).
- [X] Define stale-data and fallback behavior when source fails.

### Day 10: Workflow protocol
- [X] Scaffold Next.js + TypeScript.
- [X] Add folders/contracts from the spec and env config only.
- [X] Define daily workflow and commit checkpoint rules.
- Daily workflow (1 hour/day):
- Pick one scoped task with a clear done condition before coding.
- Touch at most two files.
- Run task-specific validation while working.
- Before each commit: run `lint`, `test`, code review, `npm run dev`, and update the roadmap if project status changed before suggesting a commit message.
- If blocked for more than 15 minutes, capture blocker + next action and stop.

### Day 11–12: Repo + contracts in code
- [X] Add `IDataSource` and app shell.
- [X] Implement public-data adapter + normalization pipeline.
- [X] Add basic cache and status endpoint.
- Edge case note: source health probes bypass cache to avoid reporting stale "healthy" status.

### Day 13–14: Public source adapter
- [X] Write 30–50 NL examples with expected intent/slots.
- [X] Mark easy/medium/hard + failure cases.

### Day 15–17: Query parser v1
- [X] Implement intent/slot extraction + alias resolution.
- [X] Add low-confidence and ambiguity responses.
- [X] Define clarification payload contract (`reason`, `prompt`, `slot`, `candidates`, `confidence`).
- [X] Set confidence thresholds for `answer` vs `clarify` vs `reject`.
- [X] Add parser regression tests for ambiguity and low-confidence flows.

### Residual-risk mitigation [X]
- [X] Add telemetry logging for unmatched alias tokens.
- [X] Add telemetry logging for unknown comparator phrases.
- [X] Expand comparator lexicon mapping and add parser regression tests.
- [X] Expand alias coverage with normalized team/player lookup entries and add regression tests.

### Day 18–20: Query API route
- [X] Implement `POST /api/query`.
- [X] Map parsed intent to data calls via adapter.
- [X] Return structured response (`results`, `summary`, `needsClarification`).

### Day 21–23: UI MVP
- [X] Build search UI, result cards/table, loading/error/empty states.
- [X] Add recent query history and mobile layout baseline.

### Day 24–25: Query coverage expansion
- [X] Add more NL patterns: leaders, team/game, player seasonal and weekly views.
- [X] Add sort/limit filters.

### Day 26–27: Performance + hardening
- [X] Add server-side cache + throttle safeguards for upstream 5 requests/min free-tier limits.
- [X] Add caching knobs, timeout/retry tuning, optional autocomplete endpoints.
- [X] Run parser corpus against implementation and patch top failures.

### Day 28: Final QA + deploy prep
- [X] Reduce top parser corpus mismatch buckets (scopeType:career, intent:leaders, sort:desc, state:clarification, state:error) and rerun corpus to record delta.
  - Corpus rerun is now scripted in `scripts/evaluate-parser-corpus.mjs`.
  - Delta vs 2026-03-19 snapshot after parser fixes + corpus expectation audit: `intent:leaders` 7 -> 0, `scopeType:career` 7 -> 0, `sort:desc` 4 -> 0, `state:clarification` 4 -> 0, `state:error` 3 -> 0.
  - Unsupported-state audit updated stale corpus rows that still expected `clarification` for explicitly unsupported queries, and the corpus now evaluates cleanly.
- [X] Run full manual QA pass on corpus.
- [X] Fix named-player stat lookups by resolving snapshot roster identities before querying abbreviated stat rows.
- [X] Fix team-scoped follow-up context so carried team/stat/season context constrains leader queries.
- [X] Route matchup-summary phrasing to weekly summary instead of compare when no stat is requested.
- [X] Rename query response source labeling and refresh stale nflverse/UI copy.
- [X] Add an app icon so page loads no longer emit `/favicon.ico` 404 noise.
- [X] Prepare deploy config.
- [X] Prepare README.
- [X] Add stale cache fallback for source failures and expose `dataStale` indicators in query responses.
- [X] Make hosting health checks cache-aware or internal-only so deploy probes do not consume upstream request budget.
- [X] Remove build-time source-health snapshot from homepage and move source-health checks to a client-triggered flow.
- [X] Investigate and remove remaining build-time upstream `teams` requests during static generation.
- [X] Address external review findings: clarification context wiring, season aggregation, pagination completeness, and truncation safeguards.
- [X] Separate source failures from clarification so unsupported upstream failures show as a distinct state in API responses and UI.
- [X] Add source-health regression test that verifies probes call the uncached freshness path (`getTeamsFresh`) instead of the cached path.
- [ ] Expand comparator lexicon and add phrase-level regression tests for uncommon ranking phrasing.
- [X] Add GitHub Actions CI for format, lint, tests, and build.
- [X] Replace the live upstream stats dependency with a build-time nflverse SQLite snapshot flow.
- [X] Migrate SQLite runtime access to `better-sqlite3`.
- [X] Pin validation/runtime docs to Node 24 + npm 11.
- [X] Extend CI to rebuild the nflverse snapshot from a clean checkout and verify SQLite schema/metadata before tests and app build.
- [X] Add a Docker-based deploy path so the verified sqlite snapshot is baked into the deploy artifact instead of rebuilt on the host.
- [X] Deploy preview, smoke test, record known limitations.
  - Current deploy blockers addressed in code:
    - Next.js route modules cannot export test helpers, so `/api/query` test injection was moved out of `src/app/api/query/route.ts` into a separate helper module.
    - The repo uses explicit `.ts` import specifiers for the Node ESM test/runtime flow, so `tsconfig.json` now enables `allowImportingTsExtensions` for `next build` compatibility.

### Long Term Tasks
- [X] Separate upstream/source failures from clarification in the UI so temporary data issues do not render as "Needs clarification".
- [X] UI overhaul: redesign the website for a stronger production-quality visual system and layout.

## Residual Risks (living section)
- Last updated: 2026-03-26
- [X] Alias coverage baseline expanded to 32 teams and >=500 players, and broad false-positive team tokens (`bay`, `las`, `new`, `city`) were removed.
  - Next mitigation: add periodic alias-refresh generation so roster changes and new names stay current.
- [X] Unsupported-domain and unsupported-scope queries now resolve to an explicit unsupported state instead of falling through clarification.
  - Next mitigation: expand unsupported-domain cue coverage as the corpus grows.
- [ ] Comparator parsing is still keyword-based, so uncommon ranking phrasing may map to incorrect sort direction.
  - Next mitigation: expand comparator lexicon and add phrase-level regression tests.
- [ ] Snapshot freshness depends on the last successful `npm run build:snapshot`, so stale deploy artifacts can lag nflverse updates.
  - Next mitigation: keep deploy smoke checks pointed at the built season and rebuild container images on a schedule.

## Parser Corpus Bucket Tracker (snapshot 2026-03-20)
- Evaluator: `node --experimental-strip-types scripts/evaluate-parser-corpus.mjs`
- Current audited corpus result: `0` mismatches across `60` samples.
